<UserControl x:Class="DesktopWidgets.UserControls.StopwatchClock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DesktopWidgets.UserControls"
             xmlns:desktopWidgets="clr-namespace:DesktopWidgets"
             mc:Ignorable="d">
    <UserControl.Resources>
        <desktopWidgets:DateTimeToElapsedTimeTextConverter x:Key="DateTimeToElapsedTimeTextConverter" />
        <desktopWidgets:BoolToStartStopTextConverter x:Key="BoolToStartStopTextConverter" />
    </UserControl.Resources>
    <StackPanel>
        <TextBlock TextAlignment="Center">
            <TextBlock.Text>
                <MultiBinding Converter="{StaticResource DateTimeToElapsedTimeTextConverter}">
                    <Binding Path="CurrentTime" />
                    <Binding Path="StartTime" />
                    <Binding Path="Settings" />
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
        <Button Content="{Binding Path=IsRunning, Converter={StaticResource BoolToStartStopTextConverter}}"
                Command="{Binding StartStopCommand}" />
    </StackPanel>
</UserControl>