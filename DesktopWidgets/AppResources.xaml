<ResourceDictionary x:Class="DesktopWidgets.AppResources"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DesktopWidgets"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:tb="http://www.hardcodet.net/taskbar"
                    xmlns:p="clr-namespace:DesktopWidgets.Properties"
                    xmlns:classes="clr-namespace:DesktopWidgets.Classes">
    <classes:BoolToEnableDisableNameConverter x:Key="BoolToEnableDisableNameConverter" />
    <x:Array x:Shared="False" Type="{x:Type sys:Object}" x:Key="MainMenuItems">
        <MenuItem Header="Manage Widgets..." Click="menuItemManageWidgets_OnClick" />
        <MenuItem Header="Options..." Click="menuItemOptions_OnClick" />
        <Separator />
        <MenuItem Header="Check for Updates..." Click="menuItemCheckForUpdates_OnClick" />
        <Separator />
        <MenuItem Header="Exit" Click="menuItemExit_OnClick" />
    </x:Array>
    <CompositeCollection x:Shared="False" x:Key="WidgetMenuItems">
        <MenuItem Header="Edit..." Command="{Binding EditWidget}" />
        <Separator />
        <MenuItem Header="Bring To Front" Command="{Binding WidgetBringToFront}" />
        <MenuItem Header="Drag to Move" IsChecked="{Binding Settings.DragToMove}" IsCheckable="True"/>
        <MenuItem Header="Stay On Top" IsChecked="{Binding Settings.OnTop}" IsCheckable="True"/>
        <MenuItem Header="Dock">
            <MenuItem Header="None" Command="{Binding WidgetDockPosition}" CommandParameter="{x:Static local:ScreenDockPosition.None}" />
            <MenuItem Header="Position">
                <MenuItem Header="Left" Command="{Binding WidgetDockPosition}" CommandParameter="{x:Static local:ScreenDockPosition.Left}" />
                <MenuItem Header="Right" Command="{Binding WidgetDockPosition}" CommandParameter="{x:Static local:ScreenDockPosition.Right}" />
                <MenuItem Header="Top" Command="{Binding WidgetDockPosition}" CommandParameter="{x:Static local:ScreenDockPosition.Top}" />
                <MenuItem Header="Bottom" Command="{Binding WidgetDockPosition}" CommandParameter="{x:Static local:ScreenDockPosition.Bottom}" />
            </MenuItem>
            <MenuItem Header="Alignment">
                <MenuItem Header="Center" Command="{Binding WidgetDockAlignment}" CommandParameter="{x:Static local:ScreenDockAlignment.Center}" />
                <MenuItem Header="Top" Command="{Binding WidgetDockAlignment}" CommandParameter="{x:Static local:ScreenDockAlignment.Top}" />
                <MenuItem Header="Bottom" Command="{Binding WidgetDockAlignment}" CommandParameter="{x:Static local:ScreenDockAlignment.Bottom}" />
                <MenuItem Header="Stretch" Command="{Binding WidgetDockAlignment}" CommandParameter="{x:Static local:ScreenDockAlignment.Stretch}" /> 
            </MenuItem>
        </MenuItem>
        <Separator />
        <MenuItem Header="Reload" Command="{Binding ReloadWidget}" />
        <MenuItem
            Header="{Binding Settings.Disabled, Converter={StaticResource BoolToEnableDisableNameConverter}}"
            Command="{Binding ToggleEnableWidget}" />
        <Separator />
        <CollectionContainer Collection="{StaticResource MainMenuItems}" />
    </CompositeCollection>
    <ContextMenu x:Shared="False" x:Key="MainContextMenu" ItemsSource="{StaticResource MainMenuItems}" />
    <tb:TaskbarIcon x:Key="TrayIcon" IconSource="icon.ico" ToolTipText="{x:Static p:Resources.AppName}"
                    ContextMenu="{StaticResource MainContextMenu}"
                    TrayMouseDoubleClick="TrayIcon_OnTrayMouseDoubleClick" />
</ResourceDictionary>